### In order to copy the theme submodule:

``` git submodule init ```
``` git submodule update ```

### To run and create a new content, run: 

``` hugo new content_type\activity.md ```


#### Note: use hugo.exe in windows

